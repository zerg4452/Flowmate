# 개인화 업무관리 툴 v1 설계서

## 1. 개요

이지미디어의 기존 업무관리 시스템인 메티는 프로젝트와 테스크 단위로 여러 직원이 함께 참여하는 구조이므로, 개발자 개인의 업무 진행도와 일별 업무시간을 관리하기에는 적합하지 않다.

본 프로그램은 개발자 개인이 프로젝트별 테스크, 진행상태, 업무 히스토리, 작업시간을 로컬에서 관리할 수 있는 Windows 데스크톱 업무관리 툴을 목표로 한다.

## 2. 배포와 저장 방식

- 배포물은 사용자가 실행할 수 있는 exe 파일 1개를 기준으로 한다.
- 실행 후 생성되는 데이터 파일은 `%AppData%` 하위 앱 전용 폴더에 저장한다.
- 로컬 저장소는 LiteDB를 사용한다.
- 서버 동기화, 로그인, 권한관리, 백업과 복원은 v1 범위에서 제외한다.

## 3. 기술 구조

- 앱 유형은 C# WPF 데스크톱 앱으로 구현한다.
- 화면 구조는 MVVM 패턴을 따른다.
- UI는 WPF 기본 컨트롤을 커스텀해서 구성한다.
- 외부 테마 라이브러리 의존성은 최소화한다.
- 최종 배포는 `.NET PublishSingleFile` 기반 단일 exe 생성을 목표로 한다.

## 4. 주요 메뉴

### 4.1 메인 업무화면

- 전체 화면 구성은 JIRA를 벤치마크한다.
- 좌측에는 주요 메뉴를 배치한다.
- 중앙에는 `To do`, `in Progress [ing]`, `in Progress [Test]`, `Done` 4개 상태 컬럼을 가로로 배치한다.
- 각 상태 컬럼 하위에는 프로젝트 그룹을 표시하고, 각 프로젝트 하위에 해당 상태의 테스크를 표시한다.
- 프로젝트로 검색하지 않은 경우 현재 등록된 모든 사용 프로젝트와 프로젝트별 테스크를 상태 컬럼 안에 표시한다.
- 테스크 클릭 시 상세 내용, 댓글, 히스토리, 작업시간 입력 영역을 딤드 레이어 팝업으로 표시한다.
- 팝업 바깥의 딤드 영역을 클릭하면 닫기 전 저장 여부를 확인한다.
- 상단 필터에서 프로젝트, 등록일 기간, 키워드를 기준으로 목록을 필터링한다.

### 4.2 프로젝트 관리

- 프로젝트명을 등록, 수정할 수 있다.
- 프로젝트 사용 여부를 설정할 수 있다.
- 사용하지 않는 프로젝트는 메인 업무화면과 통계 기본 조회에서 제외한다.
- 프로젝트별 하위 테스크를 추가할 수 있다.
- 테스크 상태는 `To do`, `in Progress [ing]`, `in Progress [Test]`, `Done` 4개 고정 상태만 사용한다.
- 프로젝트 생성 시 4개 고정 상태를 자동 생성하며 사용자가 상태를 추가, 삭제, 이름 변경하지 않는다.
- 프로젝트, 테스크, 상태, 댓글은 삭제할 수 있다. 삭제 정책은 11절을 따른다.

### 4.3 통계

- 프로젝트별 총 투자시간을 표시한다.
- 테스크별 총 투자시간을 표시한다.
- 일, 주, 월, 년 단위로 업무 할당시간을 집계한다.
- 주, 월 단위 프로젝트별 테스크 개수를 표시한다.
- 통계 화면은 차트보다 표와 요약카드 중심으로 구성한다.

### 4.4 옵션

- 앱 내부 알림 사용 여부를 설정한다.
- 오늘 작업시간 미입력 알림 시각을 설정한다.
- 기본 알림 시각은 17:30이다.
- 폰트, 글자 크기, 글자색(전경색), 배경색을 설정한다.
- 글자색과 배경색은 가독성 확보를 위해 함께 설정한다.
- 글자색과 배경색은 색상 선택 다이얼로그(컬러 피커)로 지정하거나 HEX 값을 직접 입력할 수 있다.
- 라이트, 다크, 세피아, 딥블루 테마 프리셋을 제공해 글자색과 배경색을 한 번에 적용한다.
- 창 크기, 위치, 최대화 상태를 저장하고 앱 재실행 시 복원한다. 저장된 위치가 화면 밖이면 기본 위치로 연다.
- 옵션 영역 안에 휴지통(삭제 항목 관리) 화면을 제공한다. 상세는 11.6을 따른다.
- 설정값은 LiteDB에 저장하고 앱 재실행 후에도 유지한다.

## 5. 데이터 모델

소프트 삭제 대상 컬렉션(Project, ProjectStatus, Task, TaskComment, TaskHistory, TimeEntry)은 공통으로 다음 필드를 가진다.

- 삭제 여부.
- 삭제일시.
- 삭제 출처(직접 삭제 / 상위 전파 삭제 구분).

### 5.1 Project

- 프로젝트 고유 ID.
- 프로젝트명.
- 사용 여부.
- 생성일시.
- 수정일시.
- 삭제 여부, 삭제일시, 삭제 출처.

### 5.2 ProjectStatus

- 상태 고유 ID.
- 프로젝트 ID.
- 상태명.
- 정렬 순서.
- 기본 상태 여부.
- 사용 여부.
- 삭제 여부, 삭제일시, 삭제 출처.

### 5.3 Task

- 테스크 고유 ID.
- 프로젝트 ID.
- 테스크명.
- 본문 내용.
- 현재 상태 ID.
- 등록일시(시간까지).
- 수정일시.
- 완료일시(시간까지).
- 삭제 여부, 삭제일시, 삭제 출처.

메티 테스크 링크는 별도 필드로 두지 않는다. 사용자가 본문 또는 댓글에 자유롭게 URL을 입력한다.

완료일시는 테스크가 `Done` 상태로 전환된 시각으로 자동 기록한다. `Done`에서 다른 상태로 되돌리면 완료일시는 비운다.

### 5.4 TaskComment

- 댓글 고유 ID.
- 테스크 ID.
- 댓글 내용.
- 작성일시.
- 수정일시.
- 삭제 여부, 삭제일시, 삭제 출처.

### 5.5 TaskHistory

- 히스토리 고유 ID.
- 테스크 ID.
- 히스토리 유형.
- 표시 메시지.
- 변경 전 값.
- 변경 후 값.
- 생성일시.
- 삭제 여부, 삭제일시, 삭제 출처.

히스토리 유형은 댓글 추가, 상태 변경, 시간 입력, 본문 수정 등으로 구분한다.

### 5.6 TimeEntry

- 시간 입력 고유 ID.
- 테스크 ID.
- 작업일.
- 작업시간 분.
- 메모.
- 생성일시.
- 수정일시.
- 삭제 여부, 삭제일시, 삭제 출처.

작업일은 당일뿐 아니라 과거 날짜도 입력할 수 있다.

### 5.7 AppOption

- 알림 사용 여부.
- 알림 시각.
- 폰트명.
- 글자 크기.
- 글자색(전경색).
- 배경색.
- 창 너비, 창 높이.
- 창 위치(Left, Top).
- 창 최대화 여부.

### 5.8 스키마 버전

- LiteDB에 스키마 버전 값을 저장한다.
- 앱 시작 시 저장된 스키마 버전과 현재 앱 기대 버전을 비교한다.
- 버전이 낮으면 필드 추가, 기본값 채우기 같은 마이그레이션을 순차 적용 후 버전을 갱신한다.
- v1 최초 생성 시 스키마 버전은 1로 초기화한다.

## 6. 핵심 화면 동작

### 6.1 테스크 상세 팝업

- 테스크 상세에는 테스크명, 현재 상태, 본문, 등록일시, 완료일시, 총 누적시간, 댓글과 히스토리 타임라인을 표시한다.
- 테스크 상세는 메인 화면 우측 고정 영역이 아니라 레이어 팝업으로 표시한다.
- 팝업이 열리면 배경 빈 영역은 딤드 처리한다.
- 본문은 기본적으로 열람 모드로 표시한다. 수정 버튼을 클릭한 경우에만 수정 모드로 전환한다.
- 수정 모드에서 저장 또는 취소하면 다시 열람 모드로 돌아간다. 취소 시 변경 내용은 폐기한다.
- 딤드 영역 또는 닫기 버튼을 클릭하면 본문 변경 여부를 확인하고 저장, 저장 안 함, 취소 중 하나를 선택하게 한다.
- 본문 저장 시 본문 수정 히스토리를 자동 기록한다.

### 6.2 상태 변경

- 테스크는 같은 프로젝트의 상태 목록 안에서 자유롭게 상태를 변경할 수 있다.
- 상태 변경 시 Task의 현재 상태를 갱신한다.
- 상태 변경 내역은 TaskHistory에 자동 기록한다.

### 6.3 댓글과 히스토리

- 사용자는 테스크에 댓글 형태로 업무 메모를 계속 추가할 수 있다.
- 댓글 추가 시 TaskComment에 저장한다.
- 댓글 추가, 상태 변경, 시간 입력은 하나의 타임라인에 함께 표시한다.
- 히스토리는 등록일시 기준 최신 항목이 위에 오도록 내림차순 정렬한다.
- 사용자는 실수로 생성된 댓글, 히스토리 항목을 개별 삭제할 수 있다. 삭제는 소프트 삭제로 처리한다.

### 6.4 작업시간 입력

- 사용자는 테스크별로 작업일과 분 단위 작업시간을 입력한다.
- 과거 날짜에도 작업시간을 입력할 수 있다.
- 같은 날 여러 번 입력하면 덮어쓰지 않고 누적한다. 각 입력은 별도 TimeEntry로 저장한다.
- 특정 날짜의 작업시간은 해당 날짜 TimeEntry 분 합계로 계산한다.
- 입력된 시간은 테스크 총 누적시간과 프로젝트 총 누적시간에 반영한다.
- 시간 입력 시 TaskHistory에 자동 기록한다.

## 7. 필터와 통계 기준

- 메인 업무화면의 프로젝트 필터는 전체 또는 특정 프로젝트를 선택할 수 있다.
- 상태는 메인 화면의 4개 컬럼으로 항상 노출하므로 별도 상태 필터는 제공하지 않는다.
- 등록일 필터는 시작일과 종료일을 기준으로 조회한다.
- 키워드 검색을 지원한다. 검색 대상은 테스크 제목과 본문 내용으로 한정한다. 댓글, 히스토리는 검색 대상에서 제외한다.
- 키워드 입력은 디바운스(약 300ms)를 적용해 입력이 멈춘 뒤 목록을 갱신한다.
- 통계는 TimeEntry의 작업일을 기준으로 일, 주, 월, 년 단위 집계한다.
- 테스크 개수 통계는 Task의 등록일을 기준으로 집계한다.

## 8. 알림 정책

- v1 알림은 Windows 토스트 알림이 아니라 앱 내부 배너 또는 팝업으로 구현한다.
- 앱이 실행 중이고 알림 사용 옵션이 켜져 있을 때만 동작한다.
- 앱이 실행 중이 아니면 알림은 동작하지 않는다. 이는 v1 의도된 동작이다.
- 기본 시각 17:30에 당일 TimeEntry가 없으면 오늘 작업시간 미입력 알림을 표시한다.
- 사용자가 알림을 확인하면 같은 날에는 반복 표시하지 않는다.

## 9. 제외 범위

- 서버 기반 동기화.
- 로그인과 사용자 권한관리.
- 여러 PC 간 데이터 공유.
- 백업과 복원.
- Windows 토스트 알림.
- 차트 중심 통계 화면.
- 모바일 또는 웹 버전.

## 10. 검증 기준

- 단일 exe 배포물이 생성되어야 한다.
- 최초 실행 시 AppData 하위에 LiteDB 데이터 파일이 자동 생성되어야 한다.
- 프로젝트 생성, 수정, 사용 여부 변경이 정상 동작해야 한다.
- 프로젝트별 상태가 4개 고정 상태로 유지되어야 한다.
- 테스크 생성, 상태 변경, 본문 팝업 열람과 수정이 정상 동작해야 한다.
- 댓글, 상태 변경, 시간 입력이 히스토리에 누락 없이 표시되어야 한다.
- 과거 날짜 작업시간 입력과 총 누적시간 계산이 정상 동작해야 한다.
- 프로젝트, 등록일, 키워드 필터가 정상 동작해야 한다.
- 일, 주, 월, 년 단위 통계가 TimeEntry 기준으로 계산되어야 한다.
- 옵션 변경 후 앱 재실행 시 설정값이 유지되어야 한다.
- 삭제는 11절 소프트 삭제 정책대로 동작하고, 삭제 전 확인 절차가 있어야 한다.
- 삭제된 항목은 일반 조회와 통계에서 제외되고, 휴지통에서 복구 또는 영구 삭제할 수 있어야 한다.
- 복구 시 전파 삭제된 하위 항목이 함께 복구되어야 한다.

## 11. 삭제 정책

- v1은 소프트 삭제(soft delete)를 기준으로 한다.
- 삭제 시 레코드를 물리적으로 지우지 않고 삭제 플래그(삭제 여부, 삭제일시)만 설정한다.
- 모든 삭제는 실행 전 확인 다이얼로그를 표시한다.
- 삭제된 레코드는 메인 업무화면, 통계, 검색, 일반 조회에서 기본 제외한다.
- 삭제된 레코드의 시간은 누적시간과 통계 집계에서 제외한다.
- 삭제된 항목의 복구(정상 전환) 또는 영구 삭제는 11.6 휴지통에서 처리한다.

### 11.1 삭제 전파 규칙

- 상위 항목을 삭제하면 하위 항목도 함께 소프트 삭제로 표시한다.
- 프로젝트 삭제: 하위 테스크, 상태(ProjectStatus), 댓글, 히스토리, 시간 입력을 함께 삭제 표시한다.
- 테스크 삭제: 해당 테스크의 댓글, 히스토리, 시간 입력을 함께 삭제 표시한다.
- 전파로 삭제된 하위 항목은 상위 항목 복구 시 함께 복구할 수 있도록 전파 출처를 구분한다.

### 11.2 프로젝트 삭제

- 확인 다이얼로그에 삭제 대상 테스크 개수와 누적시간을 함께 표시한다.
- 단순히 화면에서 숨기려는 경우는 삭제가 아니라 프로젝트 사용 여부 설정을 사용한다.

### 11.3 테스크 삭제

- 삭제된 시간은 프로젝트 누적시간과 통계에서 제외된다.

### 11.4 상태 삭제

- v1에서는 상태 삭제를 제공하지 않는다.
- 상태는 `To do`, `in Progress [ing]`, `in Progress [Test]`, `Done` 4개 고정값으로 유지한다.

### 11.5 댓글과 히스토리 삭제

- 사용자가 직접 추가한 댓글은 삭제할 수 있다. 삭제한 댓글은 휴지통에서 복구할 수 있다.
- 실수로 생성된 히스토리 항목도 개별 삭제할 수 있다. 삭제 전 확인 다이얼로그를 표시한다.
- 댓글, 히스토리 삭제는 소프트 삭제로 처리하며 일반 타임라인에서 즉시 제외한다.

### 11.6 휴지통 (삭제 항목 관리)

- 옵션 영역 안에 휴지통(삭제 항목 관리) 화면을 둔다.
- 삭제 플래그가 설정된 프로젝트, 테스크, 상태, 댓글 목록을 종류별로 표시한다.
- 각 항목은 삭제일시를 함께 표시한다.
- 각 항목에 대해 두 가지 동작을 제공한다.
  - 정상으로 복구: 삭제 플래그를 해제해 원래 상태로 되돌린다. 전파로 삭제된 하위 항목도 함께 복구한다.
  - 영구 삭제: 레코드를 물리적으로 완전히 제거한다. 복구 불가.
- 영구 삭제는 실행 전 별도 확인 다이얼로그를 표시한다.
- 프로젝트 복구 시 같은 이름의 활성 프로젝트가 있어도 충돌 없이 복구한다. 이름 중복은 허용한다.
- 프로젝트명, 상태명 등 이름 필드에는 유니크 인덱스를 걸지 않는다. 모든 관계는 `_id` 기반 외래키로 종속하므로 이름 중복은 키 충돌을 일으키지 않는다.
